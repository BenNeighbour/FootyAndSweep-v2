spring:
  cloud:
    kubernetes:
      reload:
        period: 1000
        enabled: true

      secrets:
        name: api-allocation-engine-credentials

      config:
        enabled: true
        sources:
          - namespace: default
            name: api-allocation-engine-config