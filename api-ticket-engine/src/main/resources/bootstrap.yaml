spring:
  cloud:
    kubernetes:
      reload:
        period: 1000
        enabled: true

      secrets:
        name: api-ticket-engine-credentials

      config:
        enabled: true
        sources:
          - namespace: default
            name: api-ticket-engine-config