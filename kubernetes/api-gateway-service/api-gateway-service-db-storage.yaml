kind: PersistentVolume
apiVersion: v1
metadata:
  name: api-gateway-service-db-volume
  labels:
    type: local
    app: api-gateway-service-db
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: api-gateway-service-db-claim
  labels:
    app: api-gateway-service-db
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi