apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-events-cdc
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-events-cdc
  template:
    metadata:
      labels:
        app: api-events-cdc
    spec:
      volumes:
        - name: data
          emptyDir: {}
      containers:
        - name: redis
          image: redis:5.0.7
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/
---
apiVersion: v1
kind: Service
metadata:
  name: api-events-cdc
  namespace: default
spec:
  type: NodePort
  ports:
    - port: 6379
  selector:
    app: api-events-cdc