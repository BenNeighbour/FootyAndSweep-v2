spring:
  cloud:
    kubernetes:
      reload:
        period: 1000
        enabled: true

      secrets:
        name: api-authentication-service

      config:
        enabled: true
        sources:
          - namespace: default
            name: api-authentication-service-config