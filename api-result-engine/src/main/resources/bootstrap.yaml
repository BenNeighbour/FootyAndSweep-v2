spring:
  cloud:
    kubernetes:
      reload:
        period: 1000
        enabled: true

      secrets:
        name: api-sweepstake-engine-credentials

      config:
        enabled: true
        sources:
          - namespace: default
            name: api-result-engine-config